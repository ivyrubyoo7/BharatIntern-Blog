<!DOCTYPE html>
<html>
<head>
    <title>Blog-O-paedia</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <style>
        body {
            margin: 0;
            background-color: whitesmoke;
        }
        .h1 {
            margin: 0;
            text-align: center;
            padding: 100px;
            font-size: 30px;
            background: linear-gradient(180deg, rgba(45, 44, 44, 0.8), rgba(31, 26, 26, 0.6)), url('https://cdn.wallpapersafari.com/53/65/9Vzvjn.jpg');
            color: white;
        }
        #comp {
            display: block;
            margin: 30px;
            padding: 10px;
            margin-left: auto;
            margin-right: auto;
            border-radius: 50px;
            border: 1px solid black;
            width: fit-content;
            background-color: blue;
            color: white;
        }
        a {
            color: darkblue;
        }
        .h2 {
            border: 1px dashed black;
            margin: 20px;
            padding: 10px;
        }
        h2 {
            font-size: 30px;
            font-style: italic;
        }
        .post-content {
            white-space: pre-wrap; 
            word-wrap: break-word; 
            font-family: Arial, sans-serif; 
            font-size: 1em; 
            color: #333; 
            background-color: #f8f8f8; 
            padding: 10px; 
            border: 1px solid #ddd;
            border-radius: 5px; 
        }
        .delete-btn {
            color: white;
            background-color: red;
            border: none;
            padding: 10px;
            cursor: pointer;
            border-radius: 5px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="h1">
        <h1 style="color: white;">Welcome to the Blog-O-paedia</h1>
        <h6>
            Here you can post your latest and viral blogs for information and help others to gain knowledge with your experience.
        </h6>
    </div>
    <a href="/compose" id="comp">Publish a new Post</a>
    <% posts.forEach(function(post) { %>
        <div class="h2">
            <h2><a href="/posts/<%= post._id %>"><%= post.title %></a></h2>
            <pre class="post-content"><%= post.content %></pre>
            <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="delete-btn">Delete</button>
            </form>
        </div>
    <% }); %>
</body>
</html>
